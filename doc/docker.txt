# Delete every Docker containers
# Must be run first because images are attached to containers
docker rm -f $(docker ps -a -q)

# Delete every Docker image
docker rmi -f $(docker images -q)




docker rmi -f $(docker images | grep '^<none>' | awk '{print $3}')

docker rm -f $(docker ps -a -q)

docker rm -f $(docker ps -a | grep 'jwilder/nginx-proxy' | awk '{print $1}')



docker volume rm $(docker volume ls -qf dangling=true)

$ docker volume ls -qf dangling=true
docker volume ls




docker pull will fail if docker service is not running. Make sure it is running by

:~$ ps aux | grep docker
root     18745  1.7  0.9 284104 13976 ?    Ssl  21:19   0:01 /usr/bin/docker -d
If it is not running, you can start it by

sudo service docker start

For Ubuntu 15 and above use

sudo systemctl start docker


docker-error-var-run-docker-sock-no-such-file-or-directory


docker rm -f $(docker ps -a | grep 'jwilder/nginx-proxy' | awk '{print $1}') && docker run -d -p 80:80 -v /var/run/docker.sock:/tmp/docker.sock:ro  -v /root/docker-data/nginx/nginx.conf:/etc/nginx/nginx.conf:ro jwilder/nginx-proxy && docker ps -a


1. digitalocean 
2. centos 7emailssh
2-1. http://get.daocloud.io/#install-docker
3. yum -y install docker
service docker start
chkconfig docker on
4. yum install git
5. yum install bzip2
6. centos_env.sh
7. vi /usr/bin/yum 
change python to python_old

8. vi /etc/profile
export PATH="/opt/conda/bin:${PATH}"

9. docker_compose_version=1.13.0
curl -L https://github.com/docker/compose/releases/download/${docker_compose_version}/docker-compose-`uname -s`-`uname -m` > /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

10 docke-compose up -d && docker-compose ps